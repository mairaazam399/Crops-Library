# Simple web frontend (Next.js) for Crops Library

This is a minimal Next.js app that lets graders open the web UI, upload an image, and call the backend /identify endpoint.

Run locally:
1. cd web
2. npm install
3. npm run dev

Change the BACKEND_URL in pages/index.js if your backend is not running at http://localhost:8000
